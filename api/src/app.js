// const express = require('express');
// const cors = require('cors');
// const helmet = require('helmet');
// const path = require('path');
// const morgan = require('morgan');
// const app = express();

// app.use(helmet());// nos permite proteger la aplicacion de ataques
// app.use(cors());// nos permite que la aplicacion sea accesible desde cualquier origen
// app.use(express.json())// nos permite que la aplicacion entienda los datos en formato json
// app.use(express.urlencoded({ extended: true }))// nos permite que la aplicacion entienda los datos en formato urlencoded
// app.use(morgan("combined"))// nos permite que la aplicacion muestre los datos que se estan enviando
// app.use(express.static(path.join(__dirname, 'public')));//

// module.exports = app
